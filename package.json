{
  "name": "shopuz-frontend-deploy",
  "private": true,
  "scripts": {
    "build": "npm --prefix vue ci && npm --prefix vue run build",
    "start": "npm --prefix vue run preview -- --host 0.0.0.0 --port $PORT"
  }
}